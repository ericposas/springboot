{
  "name": "springboot-api",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start:web": "yarn build && docker system prune -f && docker compose -f docker-compose.yml -f docker-compose-web.yml up springboot-api",
    "start:dev": "docker compose up",
    "build": "docker build -t webdev00/springboot-api:latest . -f web.Dockerfile && docker push webdev00/springboot-api:latest",
    "watch": "onchange 'build.gradle' 'src/**/*' -- docker restart springboot-api"
  },
  "dependencies": {
    "onchange": "^7.1.0"
  }
}
